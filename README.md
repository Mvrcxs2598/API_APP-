Using Railway as a public URL domain.

Railway todo: 

Railway pulls the code from github.
Creates a runable image, and starts the web server. 
Has a postgres DB service. 
Public URL: 

DB_volume(local): apiget_pgdata
DB: api_app
